meta {
  name: Login - Technician
  type: http
  seq: 2
}

post {
  url: {{base_url}}/login
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "phone": "09111111101",
    "password": "password",
    "device_name": "bruno-technician"
  }
}

script:post-response {
  const data = res.getBody();

  if (data.access_token) {
    bru.setEnvVar("token", data.access_token);
  }
}
